# Managed by stuttter/.github fleet standards. Do not edit locally.
name: Plugin CI

on:
  pull_request:
  push:
    branches:
      - "master"

permissions:
  contents: read

jobs:
  validate:
    uses: stuttter/.github/.github/workflows/wordpress-plugin-ci.yml@3d69828a02fd370dacce1e3fc871a0a3922807b2
    with:
      php-versions: '["7.4","8.0","8.2","8.4"]'
      quality-php-version: '8.4'
