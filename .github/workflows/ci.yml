# Managed by stuttter/.github fleet standards. Do not edit locally.
name: Plugin CI

on:
  pull_request:
  push:
    branches:
      - "master"

permissions:
  contents: read

jobs:
  validate:
    uses: stuttter/.github/.github/workflows/wordpress-plugin-ci.yml@dcaf30eb5819008a6b6e05b91ed54b8334bba64a
    with:
      php-versions: '["7.4","8.0","8.2","8.4"]'
      quality-php-version: '8.4'
